※Readmeに書くような情報ではないものや、開発の裏話的な何か。  

# 動作確認済みの環境とその考察  

Tauriでの開発でもっとも厄介なのは、各OSのWebViewの挙動に振り回されるところではないかと思う。  

一応、OS間の差異を吸収するためにVersoブラウザ(Servoベース)を搭載するプロジェクトとかもあるそうだが、肝心のServoがまだアルファ版の段階なので、現状の助けにはなりそうもない。CEF（Chromium Embedded Framework）なんてものもあるみたいだが、こっちも現状では難しそうである。  

## Windows  
◯Win11（Core i5 13400F, 32GB RAM, RTX3060）  

Tauri移植の恩恵を最も受けたのはWindows版で、懸案だった起動速度が劇的に速くなった。  

WebView2のおかげで描画も安定しており、目立った不具合もない。Tauriとの相性がもっとも良いのはWindowsのようである。今後もこれを軸に開発することになるだろう。  

## MacOS  
△MacOS Tahoe（Mac mini M4, 16GB Unified Memory）  

Electronでは優等生だったMacOSだが、Tauri版の開発は苦労が多い。関連付けでテキストファイルを開くだけでもopen-fileイベントを拾うための専用の処理を書かなければならないのだが、加えてWebKit (Safari) とTauriの相性があまり良くないようで、範囲選択時の不具合や、ウィンドウの角丸めが効かない（解決済み）問題など、いくつかの問題に悩まされた。  

またMacはMirrorShard無印（Electron）の起動も割と高速で、無理にTauriに移植する意味が薄いのではないかという疑問もあった（ファイルサイズは劇的に小さくなるが）。

……というわけで一時は開発停止まで視野に入れていたのだが、どうやら難所は抜けたようで、現在は開発中にそこまでの苦労はない。現状ではなんだかんだで（範囲選択の問題以外は）おおむね正常に動作しており、開発者自身の普段使いのエディタとして日々活躍している。  
機能の追加も進んでいるため、すでにElectron版よりこちらの方が使い勝手が良い。  

懸案だった範囲選択の問題は、Tauriのアップデートもあってある程度の改善に成功した。スクロールバーをドラッグして広範囲を選択したりすると正しく動作しないのだが、それ以外は改善されたように思える。全選択もできるようになった。  

### 追記
現時点（v1.11.0リリース）で初版リリースから８ヶ月ほど経過したが、開発者自身が普段遣いする中で、これといって大きな問題は起きていない。  
ただ、最近になってようやく気づいたのだが、Macでは縦書きプレビューの終了時の位置を適切に復元できていないらしい。  
バックエンドで非表示で生成したウィンドウをフロントエンドでshow()すると、OSの仕様でカスケーディング配置されてしまうらしいのだが、しかしこれを直そうとしてバックエンド側でshow()すると、画面が描画された後に重い初期化処理が走るため、フリッカーなどの問題が起こる可能性がある。  
開発者自身のMac Mini M4では特に問題は起きなかったが、他の環境でどうなるかがわからないため現状の仕様を維持することにした。ご了解いただければ幸いである。  

## Linux  
△MX Linux（Core i7 9700, 24GB RAM, グラフィックボードなし）  
×MX Linux（Core i7 7700, 16GB RAM, GTX1050Ti）
△MX Linux（Raspberry Pi 5, 8GB RAM）  
×Raspberry Pi OS（Raspberry Pi 5, 8GB RAM）  

残念ながら、Linux版のバイナリは配布を停止した。正常に動作しない環境が多いからである。  

上記の通りDebian系のディストリビューションしか試していないのだが、WaylandやNvidiaのグラフィックボードを使用した環境では画面を表示することすらできず、起動オプション（WEBKIT_DISABLE_COMPOSITING_MODE=1など）をつければ一応動作はするものの、それでもやはり描画周りに不具合が多い。  

「X11環境」「グラフィックボードなし」という条件下なら動作はするが、それでもIME周りの表示バグがあり、半透明ウィンドウとスポットライトモードを併用したときなどに特定条件下でバグが発生することも確認している。  
また、マークダウンプレビューウィンドウも起動しない。  

ただ動作自体は軽く、Raspberry Piでも結構軽快に動作する。Raspberry Pi用の文書作成ソフトなんてものはそれほど多くないだろうし、また巨大テキストファイルを扱えるエディタはそれなりに希少なので、「巨大テキストの扱える軽量アウトラインプロセッサ」として運用するならそれなりに有用かもしれない。  

なお開発者自身が使ってみた感触では、Linuxではダークモード（ないし黒背景の半透明ウィンドウ）のほうがきれいに描画されるように思える。ライトモード（とくに背景画像使用時）は、描画にどことなく違和感がある。  
……というより、そもそもLinuxではElectron版を使った方がいいのかもしれない。  

### 追記
その後、なんとか動作するようになったため、v1.7.0からバイナリの配布を再開した。ただ、一部の機能には制限をかけてある。  
Tauri自体も改善されたのか、起動オプションにWEBKIT_DISABLE_COMPOSITING_MODE=1をつければ基本的には普通に運用できる。ただしインライン変換が出来なかったり、背景が半透明のときに残像が出たりと、軽微ではあるものの何かしらの不具合が出ることが多い。
TauriとWebkitGTKの相性が悪い上、Linuxは実行環境がとにかく多様で、環境ごとに不具合の出方も多様なのが困りものである。  

なお、現在ではflake.nixを添付してNixOSにも対応している。開発者自身はNixOS + Niri + Noctaliaという環境で運用しているのだが、機能の多くを別ウィンドウで開くMirrorShardの実装は案外Niriと好相性なのではなかろうか、と思っている。  
またNoctaliaの洗練されたデザインは、ミニマル志向のMirrorShardと目指している方向が近く、その意味でも結構気に入っている。  

### 追記2：LinuxでもGPUコンポジットが正常動作するケース  
上述の通り、Wayland環境やNvidia製グラフィックボード使用時にはGPUコンポジットをオフにしないとまともに動作しないのだが、しかし検証の結果、なぜかNixOS + Niri + GTX 1050Ti環境ではGPU描画が正常に動作することが確認された。

ならばと思ってデスクトップをHyprlandに変更して試してみたのだが、こちらは一瞬だけウィンドウが立ち上がってすぐ消え、

Gdk-Message: 07:11:49.928: Error 71 (プロトコルエラー) dispatching to Wayland display.

というエラーだけが残された。
Gemini君によると、どうやらバックエンドで動いているWaylandライブラリの違いによって挙動に差が出るらしい。

――ということで、Waylandの主要なデスクトップをいくつか試してみた（すべてNixOS + GTX 1050Ti環境）。結果は以下である。

Hyprland (wlroots系)……Error 71  
Labwc (wlroots) ……Error 71  
GNOME (Mutter)……Error 71  
KDE Plasma (KWin)……Error 71  
Niri (Smithay)……正常に動作  
COSMIC (Smithay系)……正常に動作  
Weston (Waylandのリファレンス実装)……正常に動作  
なお、jay (Smithay系), Miriway (Mir), miracle-wm（同じ）はウィンドウマネージャ自体が正常に起動しなかった。  

……この問題はNvidia vs Waylandの相性問題だと思っていたのだが、しかし案に相違してWestonではまともに動いている。となると、問題はWayland自体ではなく、コンポジタの側にあるんじゃないかという可能性が出てきた。  
ただしRust製のSmithayは優秀で、こいつだけはなぜかNVIDIA環境でもまともに動作するらしい。  

なお、同じ環境でXfce (X11) も試してみたが、こちらは

Failed to create GBM buffer os size 1826x2732: 無効な引数です

というエラーが出て、画面が透明になってしまった。  
Gemini君によると、NVIDIAのX11ドライバが WebKitGTK の GBM 確保を拒否したことを示しているらしい。Nvidia製グラフィックボードを使用している場合、たとえX11環境だったとしてもWEBKIT_DISABLE_COMPOSITING_MODE=1を設定しなければならなかったのは、こういう理由だったようである。  

そんなわけで、とりあえずは手元で動作確認のとれたx64のNiriとCOSMICの時だけGPUコンポジットを有効にする実装に変更したが、他のデスクトップ環境でも試してみたいという方は、lib.rsのrun関数の冒頭にあるLinux専用処理をコメントアウトした上でビルドしていただければ可能である。

なお、これに伴いNiri・COSMIC限定で一部の機能制限を解除した。  
具体的には  
・スポットライトモード  
・タイプ音再生  
・マークダウン／HTMLプレビュー  
の３機能が、Windows版やMacOS版と同様に使えるようになった。

## 追記3：Waylandでインライン変換が利用可能に  
その後の検証の結果、Wayland環境ではGTK_IM_MODULEを"wayland"に設定することでインライン変換が利用可能になることが判明した。  
これで、少なくともNiri環境（私の環境ではCOSMICだと動作がいまいち不安定だった）ではほぼフルスペック（BGMのストリーミング再生だけはまだできないが）で運用できることになった。  
タイリングでウィンドウを管理し、無限にデスクトップ領域を広げられるNiriは、多数のサブウィンドウを展開するMirrorShardの仕様と相性が良い。また、いわゆるNNN Stack（NixOS + Niri + Noctalia）とはデザイン的にもよく調和するため、なかなか使い勝手のいい執筆環境が構築できるのではないかと思う。  

## 追記4：BGMのストリーミング再生  
Linux版だけBGMのストリーミング再生ができない問題だが、rodioクレートを導入してLinux版だけバックエンドで処理することによってストリーミング再生が可能になった。  
これにより、（少なくともGPUコンポジットの可能なNiri環境においては）Linux版特有の機能劣化はほぼなくなった。――しかし、余りにも独特な実装と外見の美しさに惹かれて選んだNNN Stackが、はからずもTauriアプリにとっての最適解だったというのも、なんとも妙な話ではある。  

## 追記5：GPUコンポジティングについての追試  
以下のオンボードグラフィックス環境で追試を行った。

OS: MX 25.2 Infinity x86_64  
Kernel: Linux 6.12.100+deb13-amd64  
CPU: Intel(R) Core(TM) i7-9700 (8) @ 4.70 GHz  
GPU: Intel UHD Graphics 630 @ 1.20 GHz  
Memory: 2.11 GiB / 15.49 GiB (14%)  

このマシン上でデスクトップ環境だけを入れ替えてテストしてみたのだが、

DriftWM (Smithay系)……正常に動作  
GNOME (Mutter)……正常に動作  
labwc (wlroots)……正常に動作  
Xfce4 (X11)……正常に動作  

つまりNvidiaのグラフィックボードを積んでいなければ、広範な環境で正常に動作するらしい。  
この結果を踏まえ、GPUコンポジットの有効化条件を緩和した。  

## 追記6：Niri専用処理の追加と上記の補足  
MirrorShard2は機能の大半を別ウィンドウに切り出す設計なのだが、Niriのデフォルトの挙動に任せていると、すべてのウィンドウがディスプレイサイズの半分という巨大サイズで開いてしまう。  
これでは流石に使いにくいので、ウィンドウサイズを縮小し、自動で２段組にタイリングする処理を実装した。  
Niriのためだけに400行くらいのコードを書くことになったわけだが、しかし一体誰得なんだという気はしないでもない。  

それと、追記5に関して補足がある。  
Niri環境で少し使ってみたところ、降雪エフェクトやスポットライトモードの使用時にフリーズを誘発しやすいことがわかった。  
原因や影響範囲がまだ不明なので、とりあえず安全策としてLinux全環境で降雪エフェクトとスポットライトモードを禁止した。  
Niri + Noctaliaの雰囲気と降雪エフェクトはよく似合っていただけに残念だが、安定性には代えられない。仕方のないところである。  

# 追記7：  
その後、arsalan-anwari氏の発見した手法により、Waylandの他のコンポジタでもNvidia GPU使用時のGPUコンポジットが可能になった。  
プラグインとして配布されているので、以下にリンクを示す。  
https://crates.io/crates/tauri-plugin-wayland-nvidia-quirk  

とはいえMirrorShard2の場合、高負荷時の挙動にまだ多少不安が残るため、デフォルトではNvidia GPU使用時のGPUコンポジットを一律で無効にした。  

ただし、設定ウィンドウのLinuxタブから手動でGPUコンポジットを有効にできるようにしておいた。ご自身の環境に合わせ、適切な方を選択していただければと思う。  


# コードエディタモードについて  

本エディタはあくまで文書執筆が主眼であり、コードエディタモードはあくまで補助的な機能と位置づけている。そのため重い処理は極力排し、最低限の機能しか実装していない。  
はっきり言ってしまえば、あるのはシンタックスハイライトとブラケットマッチング、AIを用いない旧型のオートコンプリートくらいなもので、リンターもフォーマッターもなく、言語対応も限られている。  
AIチャットウィンドウがあるので一応バイブコーディングのようなことはできるが、本格的な開発には向かないだろう（MirrorShardのコードもAntigravityで書かれている）。  
その後v1.1.0でAIを利用したコード補完機能を搭載したが、さすがにAntigravityやCursorのような使い方をするのは無理である。  

ただ、本エディタは起動が比較的速く、Codemirror 6を採用しているため巨大テキストの扱いにも長けている。普段使いのついでにちょっとしたコーディングをするとか、何かの巨大なログをチェックしたいとか、用途によっては使い道があるかも知れない。  

なお、本エディタのHTMLプレビュー機能を使用する際にはエディタ側をコードエディタモードにしておいた方がよい（そうしないとシンタックスハイライトが効かない）。  

ちなみにコードエディタモードの配色はTokyo Nightをベースにしているが、普通にインストールすると上手く動かなかったので配色だけをお借りしている。  
そのまま使うのも何なので、グローエフェクトと軽い透過をつけてみた。本当はもうちょっと派手に光らせてサイバーパンクなアトモスフィアにしたかったのだが、可読性を下げては本末転倒なので諦めた。  
なお、この配色は通常モードのテーマプリセットにも採用し、そちらではほぼオリジナルの配色のままになっている。  

開発者自身は割と気に入っているのだが、普通のTokyo Night Themeの方が使いやすいようなら元に戻すかも知れない。  

## 追記  
結局、ワードラップ設定の追加との兼ね合いで、画面の透過はとりやめた（横スクロール時、透過した行番号の下にコードが表示されてしまうため）。文字のグローエフェクトは残ったままだが、それ以外は普通のTokyo Nightテーマである（降雪エフェクトは適用可能だが）。  

また、v1.2.0で簡易フォーマッタを追加した。Oxfmtの採用も考えたのだが、今回は見送った。どのみちどう頑張っても本格的なIDEには勝てないので、本体機能の軽量性と高速性を維持する方が望ましいだろう。  

## 追記その2
v1.5.0でOpenCodeをショートカット起動できるようになったので、その補助としてならある程度は活用できるかも知れない。とはいえやはり高機能とは言いがたいが、MirrorShard公式サイトのコードは本機能で書いたので、全く使えないというわけでもない。……まあ、その程度の機能である。  

# BGMについて  

v1.2.0で、本エディタの添付BGMを自作（AI生成）のものに差し替えた。  

これは自作の楽曲を採用したかったというよりは、単純にファイルサイズの問題である。本作MirrorShard2はElectron版に較べて大幅にファイルサイズが削減されているが、その小さなファイルサイズのうち、かなりの部分をBGMが占めているという状況になっていた。  

お借りした楽曲はとても素敵な曲だったので残したいのは山々だったのだが、自作BGMに差し替えた結果、11.9MB（Windows版）あった配布バイナリのサイズは約6MBにまで大幅に減少した。  
Electron版のバイナリが183MBあったのに較べると、なんと30分の1以下である。Tauriというフレームワークの優秀さがうかがえる。  

また、お借りした楽曲は利用規約（素材そのものの再配布禁止）に配慮して簡単に取り出せないよう加工する必要があったが、自作音源ならその必要もないから実装もシンプルになった。  

なお、楽曲生成にはACE-step 1.5を用いている。添付画像も音楽もAI生成になったわけだが、そもそもコードの大半をAIで生成しているこのエディタにとっては、むしろ相応しいのかも知れない。  

# コードの内訳  

本エディタのリポジトリにおいて、Languages欄の表記は長らく

Typescript 98.3%  
Others 1.7%

のように表示されていた。
これは実態からかけ離れた表示だったのだが、なぜこうなっていたのかというと、上記の旧添付BGMが原因である。これが拡張子.tsのファイルに文字列化して収められていたため、その膨大な文字列がTypescriptのコードとして解釈されてしまっていたのである。  
現在（v1.2.0開発中）では、以下のように表示されている。  

TypeScript 61.3%
CSS 21.1%
HTML 10.4%
Rust 7.2% 

Rustの比率は7.2％しかないのだが、Gemini君によると、これでもTauriプロジェクトにしてはかなり高いそうである。「通常のTauriアプリはRust側を「ただの起動ランチャー」として使いがちですが、MirrorShardはファイル操作、PTY（ターミナル）、Pandoc連携、ウィンドウ制御など、バックエンド側でかなりガッツリ処理を書いていることの証明です」だそうだ。  

とはいえ根幹となるエディタライブラリにはCodeMirror6を採用しているし、AIとの通信にもGoogle公式が提供している@google/generative‑aiを使っていたりする関係上、結局はTypescriptのコード量の方が圧倒的に多いのだが。  

# アイデアプロセッサ機能について  

正式版v1.0.0リリースを以てElectron版MirrorShardからの機能移植はほとんど完了したのだが、唯一移植が済んでいないのがアイデアプロセッサ機能である。  

実装に苦労が多い割にユーザーからの反応が全くない機能ではあるのだが、しかし私自身の執筆活動においては非常に重要な機能で、これがなければ私にとっての「ぼくのかんがえたさいきょうのエディタ」は完成しない。  

実現はもう少し先になるとは思うが、少なくとも本機能の移植が完了するまでは開発を続けるつもりでいる。  

## 追記  
v1.4.0でアイデアプロセッサの実装を行い、ようやくほぼ（Scrivenerファイルの読み込み機能などいくつかを除けば）主要全機能の移植が完了した。  
自分用のエディタとして欲しい機能はほぼ一通り揃ったのだが、もう少しいろいろやってみたいこと（アイデアプロセッサの新AI機能実装とか）はあるので、開発は続行するつもりでいる。
