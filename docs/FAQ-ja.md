# ■トラブルシューティング & FAQ  

## 一般

### Q. 操作がわからなくなりました。  

**A.** F1キーを押してください。操作一覧と、対応ショートカットが表示されます。  


### Q. Mac版が起動できません。「"MirrorShard"は壊れているため開けません。」などと表示されます。  

**A1.** このエラーは私がAppleに開発者登録していないため、野良アプリとしてmacOSのセキュリティ機能（Gatekeeper）にブロックされてしまっているために発生したものです。

解決方法としては以下のようになります。

方法1：「システム設定」から手動で許可する（推奨）
(お使いのmacOSのバージョンによっては、この方法が機能しない場合があります)

アプリケーションフォルダのMirrorShard.appをダブルクリックし、エラーが表示されたら「OK」を押します。
Appleメニュー > 「システム設定」>「プライバシーとセキュリティ」を開きます。
「セキュリティ」の項目に表示される「お使いのMacを保護するために"MirrorShard"がブロックされました。」というメッセージの横の「このまま開く」ボタンをクリックします。
アプリはセキュリティ設定の例外として保存され、今後は承認済みのアプリの場合と同様に、ダブルクリックして開くことができるようになります。

方法2：ターミナルを使って、手動で「検疫」を解除する（より確実）
上記の方法で「このまま開く」ボタンが表示されない場合、あるいは「お使いのMacを保護するために〜」自体が表示されない場合は、以下の手順をお試しください。これは、macOSがダウンロード時にファイルへ付ける「検疫済」の印を、手動で取り除く安全な方法です。

ターミナルを起動します。

(アプリケーションフォルダ > ユーティリティフォルダの中にあります)
以下のコマンドを入力し、アプリケーションフォルダにあるMirrorShard 2.app のアイコンをターミナルにドラッグ＆ドロップしてパスを入力します。（パスにスペースが含まれるため、ドラッグ＆ドロップが確実です）

xattr -d com.apple.quarantine

もし手動で入力、またはコピペする場合は、以下のようにパスを引用符 " " で囲ってください。

xattr -d com.apple.quarantine "/Applications/MirrorShard 2.app"

Enterキーを押します。

（何もメッセージが表示されなければ、成功です）
これで、MirrorShard.appは、通常通りダブルクリックで起動できるようになります。

お手数ですが、上記の方法をお試しいただければ幸いです。  

**A2.** 前作（Electron版のMirrorShard）がインストールされているMac環境にMirrorShard 2をインストールしようとした際、macOSのセキュリティ機能（Gatekeeper）が誤作動し、上記のエラーが出て開けなくなるケースが報告されています。  
この現象に遭遇した場合は、以下の手順をお試しください。  

1. アプリケーションフォルダにある古い「MirrorShard.app」を削除（ゴミ箱へ移動）する。  
2. MirrorShard 2のアプリ（dmgファイル）を再度ダウンロードして、インストールし直す。  
3. 初回起動時は「右クリック ➔ 開く」で起動する。  


### Q. 文字化けしています。  

A. 本エディタでは扱えないファイルの可能性があります。ファイルの破損を防ぐため、保存せずにそのままタブを閉じてください。詳しくはnotes-ja.mdの「エンコードについて」の項目を御覧ください。  


### Q. ショートカットキーが競合します。  

**A.** MirrorShardは快適な操作のために多くのショートカットを提供していますが、一部のLinuxディストリビューションやデスクトップ環境においては、OSの標準機能とショートカットが競合する場合があります（例: Ctrl+Hが効かないなど）。  
この場合、大変お手数ですが、OS側のショートカット設定を変更していただくか、アプリ上部のUIボタンから該当の機能をご利用ください。  


### Q. Linux版で一部の機能が使えません。

**A.** Linux版は現在、試験的（experimental）なサポートとなっています。
WebKitGTKの仕様や環境依存の問題により、一部の機能は無効化されています。

これは不具合ではなく仕様となります。詳細につきましては、同梱の「linux-support-ja.md」または公式リポジトリのドキュメントをご確認ください。


### Q. F1やF2を押しても、メディアコントロールが動作してしまいます。ショートカット一覧や設定画面が開けません。  

**A.** ファンクションキーがロックされている可能性があります。一般的にはFn + Escで解除できますが、お使いのPCやキーボードによってはFn + Wで解除されたりと、挙動が異なる場合があります。詳しくはお使いのキーボードやPCのマニュアルをご参照ください。  


### Q. 背景色を変更したのに背景の色が変わりません。  

**A.** 背景画像を「なし」にしてください。背景画像があると、背景色を適用しても、画像に隠れて見えません。  


### Q. クラウド保存や、複数PCでのデータ同期はできますか？  

**A.** MirrorShard自体には、特定のクラウドサービスと連携する機能はありません。しかし、多くのクラウドストレージサービスと組み合わせてご利用いただけます。  
簡単な方法:  
作成したファイルを、お使いのPCにインストールされている iCloud Drive, Google Drive, OneDrive などの同期フォルダ内に保存してください。これにより、ファイルは自動的にクラウドにバックアップされ、同じサービスを使っている他のPCとも同期されます。  
より高度な方法（上級者向け）:  
GitHub Desktop のようなバージョン管理ツールを使うと、クラウドへのバックアップに加えて、「変更履歴の保存（いつでも過去の状態に戻せる）」や、より確実な複数端末での同期が可能になります。  
導入には専門的な知識が必要なため、ご興味がおありの場合は解説サイトを検索したり、AI（ChatGPT、Google Geminiなど）に手順を尋ねてみたりすることをお勧めします。  
MirrorShardにはターミナル機能が内蔵されていますので、慣れている方ならこちらからgitを操作して高度な同期を行うこともできます。  


### Q. ファイルを上書き保存すると、ファイルの「作成日時」が「更新日時」と同じになってしまいます。これは仕様ですか？  

**A.** はい、これはMirrorShardが採用している安全な保存機能（アトミックセーブ）による仕様です。  
【なぜこのようになるのか？】  
MirrorShardは、ユーザーの大切なデータを不慮の事故から守ることを最優先に設計されています。そのため、ファイルを上書き保存する際、内部的には以下のような手順を踏んでいます。  
　１）まず、書き込む内容を全く別の一時ファイルとして完全に保存します。  
　２）一時ファイルへの書き込みが成功したことを確認してから、その一時ファイルを元のファイルにリネーム（置き換え）します。  
この方式により、もし保存の瞬間にPCがクラッシュしたり、停電が起きたりしても、元のファイルが破損したり、内容が消えてしまったりといった最悪の事態を防ぐことができます。  
【作成日時への影響について】  
この「リネームによる置き換え」という手法の特性上、OSはこれを「新しいファイルが作成された」と認識するため、ファイルの「作成日時」が更新されてしまいます。  
これは、ユーザーのデータの安全性を最大限に確保するためのトレードオフであることを、何卒ご理解いただけますと幸いです。  


### Q. もし、ファイルの作成日時をどうしても維持したいファイルがある場合は？  

**A.** お手数ですが、MirrorShardで編集する前にファイルのバックアップ（コピー）を作成しておくか、ファイル自身に作成日を書き記していただければ（「○○○○年○○月○○日作成」など）幸いです。  


## AI関連

### Q. AI機能が動作しません。

**A.** 以下の点をご確認ください。

・クラウドAIを使用する場合：APIキーが正しく設定されているか  
・ローカルAIを使用する場合：LM StudioやOllamaなどのサーバが起動しているか  
・AIセレクタで正しいAIが選択されているか  

これらのいずれかが満たされていない場合、AI機能は動作しません。

### Q. なぜMirrorShardのAI機能は自動で文章補完を行わないのですか？  

**A.** MirrorShardは「AIはあくまで道具であり、主導権は常にユーザーにあるべき」という設計思想に基づいています。  
多くのAIエディタは、リアルタイムで補完候補を提示するために、バックグラウンドで常にテキストを監視し、クラウドに送信しています。  
これは便利である一方で、思考の流れがAIに引きずられたり、意図しないタイミングで介入されたりすることがあります。  
MirrorShardでは、ユーザーが明示的に操作したときにのみAIが動作します。  
それまでは、AIは一切介入しません。  

これにより、

* 思考の主導権をユーザー自身が保てる
* 必要なときだけAIを使える
* 意図しないデータ送信を防げる

という利点があります。  
なお、リクエスト時に通信が発生するため、応答までに数秒の待ち時間が生じる場合がありますが、これは上記の設計を優先した結果です。  


## ターミナル関連  

### Q. 内蔵ターミナル (Git Bash) で `pnpm dev` などを実行すると `MODULE_NOT_FOUND` エラーになります (Windows)  

**A.** Windows環境において、内蔵ターミナルから `pnpm` 経由でスクリプトを実行すると、シンボリックリンクの解決に失敗してエラーになる場合があります。  
この現象が起きた場合は、開発しているプロジェクトのフォルダ直下に `.npmrc` というファイルを作成し、以下の1行を記述してください。  

`node-linker=hoisted`  

その後、`node_modules` フォルダを一度削除し、再度 `pnpm install` を実行することで正常に起動するようになります（シンボリックリンクの代わりに実体ファイルが展開されます）。  

### Q. PowerShellで「スクリプトの実行が無効になっている」というエラーが出ます (Windows)  

**A.** Windowsのデフォルトのセキュリティ設定により、PowerShellで `npm` や `pnpm` 等のスクリプト実行がブロックされている状態です。  
設定画面（F2）の「Terminal Settings > Shell Path」に `bin\bash.exe`（Git Bash）を指定して使用するか、Windowsの「実行ポリシー（Execution Policy）」を変更する必要があります。  
（実行ポリシーの変更については、Microsoftの公式ドキュメント等をご参照ください）  

## OpenCode関係  

### Q. OpenCodeをインストールしたのに、Ctrl+Shift+Kで起動できません。  

**A.** 環境変数をご確認ください。OpenCodeのインストールフォルダがPathに通っていない可能性があります。  

## SillyTavern関係  

### Q. 起動すると、標準ブラウザが勝手に開いてしまいます。  

**A.** SillyTavernフォルダ内の config.yaml で、 browserLaunch: enabled: false に設定してください。  

## その他  

### Q. なんでコードエディタモードはC++にもJavaにも対応していないのに、Rustには対応しているんですか？  

**A.** MirrorShardの6％くらいはRustでできているからです。  